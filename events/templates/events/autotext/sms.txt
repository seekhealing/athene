{% autoescape off %}{% now "Ymd" as today %}{{ calendar.sms_opening_override|default:calendar.default_sms_opening }}{% for event in events %}
{% if event.start_dt|date:"Ymd" == today %}Today ({{ event.start_dt|date:"l, F j" }}){% else %}{{ event.start_dt|date:"l, F j" }}{% endif %}, {{ event.start_dt|date:"P" }}:
{{ event.summary }}{% if event.location %} ( {{ event.location_name|default:event.location }} ){% endif %}
{% endfor %}
{% regroup events|dictsort:"location_name" by location_name as location_list %}{% for location in location_list %}
{% if location.grouper %}
{{ location.grouper }} is at {{ location.list.0.location_place }}{% endif %}{% if location.grouper == "SeekHealing" %}
(Ring the doorbell at the back entrance of the United Way Building)
{% elif location.grouper == "Making Whole" %}
(Go around back behind the building, across from Hi-Wire){% endif %}{% endfor %}
{{ calendar.sms_closing_override|default:calendar.default_sms_closing }}
Reply STOP to unsub.{% endautoescape %}
